package fr.imag.adele.cadse.core;

public interface EventsManager {

	/**
	 * Returns true, if the workspace is locked.
	 * 
	 * @return true, if the workspace is locked.
	 */
	public boolean isLocked();

}